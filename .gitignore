# Gemini
GEMINI.md

# Misc
.DS_Store
test/
.meta/
.env*

# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# env file
.env
.env.acctest

# Editor/IDE
.idea/
.vscode/

# TF
main.tf
terraform.tfstate
terraform.tfvars
.terraform.lock.hcl
/terraform.tfstate.backup

# Local verification harnesses (not tracked)
scripts/

# Go workspace (local SDK dev only — never committed; go.mod stays public-shape)
go.work
go.work.sum

# Agent workspace (notes, scratch, clones) — never committed
agent-space/

# Legacy standalone SDK checkout (absorbed into sdk/; kept locally only)
cortex-cloud-go/
