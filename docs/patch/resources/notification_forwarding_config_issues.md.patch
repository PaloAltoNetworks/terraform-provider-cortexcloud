14d13
< 
62,5387c61,62
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and"></a>
< ### Nested Schema for `scope.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
---
> - `and` (Attributes List) (same schema as this attribute)
> - `or` (Attributes List) (same schema as this attribute)
5394,5396c69,70
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.and.or.or`
---
> <a id="nestedatt--scope--or"></a>
> ### Nested Schema for `scope.or`
5399a74,75
> - `and` (Attributes List) (same schema as this attribute)
> - `or` (Attributes List) (same schema as this attribute)
5405,53276d80
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or"></a>
< ### Nested Schema for `scope.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--and--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--and--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.and.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or"></a>
< ### Nested Schema for `scope.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and"></a>
< ### Nested Schema for `scope.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or"></a>
< ### Nested Schema for `scope.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--scope--or--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--scope--or--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `scope.or.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
