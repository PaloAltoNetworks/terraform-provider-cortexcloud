119,120c119,120
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or))
---
> - `and` (Attributes List) (same schema as this attribute)
> - `or` (Attributes List) (same schema as this attribute)
127,53301c127,128
< <a id="nestedatt--membership_predicate--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--and--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.and.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or"></a>
< ### Nested Schema for `membership_predicate.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--and--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.and.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--and--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.and.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--and--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.and.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--and--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.and.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--and--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.and.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--and--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.and.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--and--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.and.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.or.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--and--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.and.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.and`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.and.and`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--and--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.and.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< 
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `and` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or--and))
< - `or` (Attributes List) (see [below for nested schema](#nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or--or))
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or--and"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.or.and`
---
> <a id="nestedatt--membership_predicate--or"></a>
> ### Nested Schema for `membership_predicate.or`
53304a132,133
> - `and` (Attributes List) (same schema as this attribute)
> - `or` (Attributes List) (same schema as this attribute)
53311,53321d139
< 
< <a id="nestedatt--membership_predicate--or--or--or--or--or--or--or--or--or--or--or"></a>
< ### Nested Schema for `membership_predicate.or.or.or.or.or.or.or.or.or.or.or`
< 
< Optional:
< 
< - `search_field` (String) The field to match on, for example `xdm.asset.name`.
< - `search_type` (String) The comparison to apply, for example `EQ`, `CONTAINS`, `JSON_WILDCARD` or `JSON_WILDCARD_NOT`.
< - `search_value` (String) The value to compare against.
< 
< For most search types this is a plain string. The JSON-valued search types (`JSON_WILDCARD` and `JSON_WILDCARD_NOT`) instead expect a JSON object, which must be supplied using `jsonencode(...)`; the encoded value is sent to the API as native JSON rather than as a quoted string. For example, to match a tag: `search_value = jsonencode({ key = "application", value = "databricks" })`.
