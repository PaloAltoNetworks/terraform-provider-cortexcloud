---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cortexcloud_bioc Resource - Cortex Cloud Provider"
subcategory: ""
description: |-
  Manages a single Behavioral Indicator of Compromise (BIOC) in Cortex. Backed by the upsert /public_api/v1/bioc/insert endpoint: every edit submits the server-assigned rule_id carried in state so the API overwrites the existing record in place. A change to name is just another field update on that rule_id, so it preserves the record's identity rather than recreating it. BIOC names are NOT unique per tenant: two cortexcloud_bioc resources may share the same name and will produce two distinct rule_ids.
---

# cortexcloud_bioc (Resource)

Manages a single Behavioral Indicator of Compromise (BIOC) in Cortex. Backed by the upsert `/public_api/v1/bioc/insert` endpoint: every edit submits the server-assigned `rule_id` carried in state so the API overwrites the existing record in place. A change to `name` is just another field update on that `rule_id`, so it preserves the record's identity rather than recreating it. BIOC names are NOT unique per tenant: two `cortexcloud_bioc` resources may share the same `name` and will produce two distinct `rule_id`s.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) BIOC display name. Not unique per tenant — two BIOCs may share the same name with different `rule_id`s. Changes are applied in place via the `rule_id`-keyed upsert path.
- `severity` (String) BIOC severity. Documented values come from `enums.BIOCSeverities()` — the namespaced `SEV_NNN_X` form the live API accepts on both reads and writes.
- `type` (String) BIOC category. Documented values come from `enums.BIOCTypes()`.

### Optional

- `comment` (String) Free-form comment.
- `definition` (String) JSON-encoded filter-AST object that defines the BIOC behavior (use `jsonencode(...)`). Exactly one of `xql_query` or `definition` must be set. The provider re-canonicalizes the JSON on read so cosmetic differences from server-side normalization don't surface as plan diffs.
- `mitre_tactic_id_and_name` (List of String) MITRE ATT&CK tactic identifiers (e.g. `TA0001 - Initial Access`).
- `mitre_technique_id_and_name` (List of String) MITRE ATT&CK technique identifiers (e.g. `T1059 - Command and Scripting Interpreter`).
- `status` (String) BIOC status. Accepts both lowercase (`enabled`/`disabled` per the OpenAPI enum) and uppercase (`ENABLED`/`DISABLED` as the Cortex UI writes). The provider canonicalizes to lowercase on write so plans stay quiet across a mixed UI-and-Terraform-managed tenant.
- `xql_query` (String) Raw XQL query that defines the BIOC behavior. Exactly one of `xql_query` or `definition` must be set.

### Read-Only

- `creation_time` (Number) Unix epoch milliseconds when the BIOC was first created. Stable across updates, including a rename, since the record is upserted in place rather than recreated.
- `id` (String) Mirror of `rule_id` as a string; the resource identity used for state addressing. Computed because the value is server-assigned.
- `is_xql` (Boolean) Computed: `true` when `xql_query` is set, `false` when `definition` is set. Derived from which of the two indicator representations was configured.
- `modification_time` (Number) Unix epoch milliseconds of the most recent update.
- `number_of_issues` (Number) Count of issues this BIOC has fired on. Server-maintained.
- `rule_id` (Number) Server-assigned numeric ID — the record's canonical identity. The upsert path keys on this value, so it is stable across every update, including a rename of `name`.
- `source` (String) Human-readable origin of the most recent write — typically `Public API user (key #<id>)` for API-key-authored records, or a user email for UI/SSO-authored records. Useful for detecting out-of-band drift.
